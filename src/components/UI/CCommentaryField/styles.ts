import { SxProps } from '@mui/material';

interface CCommentaryFieldStyles {
  textfield: SxProps;
}

export const cCommentaryFieldStyles: CCommentaryFieldStyles = {
  textfield: {
    width: '100%',
  },
};
